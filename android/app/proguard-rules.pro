# The app is one Activity and a WebView; nothing is reached reflectively and
# there is no JavaScript bridge, so the defaults are all that is needed.
-keepattributes SourceFile,LineNumberTable
-renamesourcefileattribute SourceFile
