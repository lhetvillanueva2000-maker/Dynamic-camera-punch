{
  "name": "dynamic-camera-punch",
  "version": "2.1.0",
  "private": true,
  "description": "Tooling for the Dynamic Camera Punch demo. The demo itself has no dependencies — it is plain HTML, CSS and JS, and opens straight from the filesystem. Playwright is here only to drive the browser tests and regenerate docs/.",
  "scripts": {
    "test": "node tools/test-demo.js",
    "capture": "node tools/capture.js"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
